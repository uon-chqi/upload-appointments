CHQI_API_USERNAME='changeme'
CHQI_API_PASSWORD='changeme'
DJANGO_ADMIN_PASSWORD='changeme'

# Generated per-install by deploy.sh and written to .env, not shared here:
#   DJANGO_SECRET_KEY      — session signing
#   FIELD_ENCRYPTION_KEY   — encrypts facility MySQL passwords at rest.
#                            Rotating it means re-entering every facility password.
